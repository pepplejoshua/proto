some_identifier
another_identifier_2
_identifier
"string"
12324
false true
'' 'c' a

let mut 
= + += - -= * *= / /= .
% %= , : ; ( ) { }
[ ] struct fn

// single line comments

> < <= >= && || | == ? != ->
for in if loop while else
not

/*
multi
line
comment
*/

break continue return
str char bool
i64
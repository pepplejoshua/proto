use other::a::b::c::d::*;
use other::[a, b, c::[d, e, f], ^::other::g];
use ^::prototyping::other::a::^::other::a;
use @arrays::Array::other::a;
use $some_module_at_root;

mod some_fns {
    pub fn public_fn() void {
        private_fn();
    }

    fn private_fn() void {
        println(1);
    }
}

use !some_fns::public_fn;

fn main() void {
    public_fn();
    other::some_mod::public_fn();
}

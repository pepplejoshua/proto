struct A {
    num : int

    fn init(n int) void {
        self.num = n
    }

    const fn s() int {
        // self.num = 3
        return self.num
    }
}


fn main() int {
    a :: new(A, 300); defer free(a)
    println(`{a}`)

    b :: some 1
    println(`{b.unwrap()}`)

    return 0
}

struct A {
    a : int

    fn init(aa int) void {
        a = aa
    }
}

fn main() int {
    int_s := new([int])
    a_ptr := new(A, 3)
    defer free(a_ptr)
    int_ptr := new(int)
    defer free(int_ptr)

    println(`int_s := {int_s}`)
    println(`A a.a := {(*a_ptr).a}`)

    return 0
}

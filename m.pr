fn arr(a [int, _]) void {
}

fn opt() ?int  {
    return some 1;
}

fn main() int {
    a :: opt();
    b : ?int : none;
    c : ?str : some "this is a string";

    println(`a = {a}, b = {b}, c = {c}.`);

    return 0;
}

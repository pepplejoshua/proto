fn main() int {
    a : Node : Node(1);
    return 0;
}

struct Node {
    n : int;

    fn init(num int) void {
        self.n = num;
    }

    fn as_str() str {
        self.does_nothing();
        return `{self.n}`;
    }

    fn does_nothing() void {}
}

fn main() int {
    arr :: [1, 2, 3, 4, 5]

    i : uint = 0
    for i < arr.len() : ({
        println(`now incrementing i`)
        i = i + 1
    }) {
        if i == 2 {
            continue
        }

        n :: arr[i]
        println(`i is {i}`)
    }

    return 0
}

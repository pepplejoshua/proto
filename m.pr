fn main() int {
    arr :: [1, 2, 3, 4, 5]

    // infinite loop
    i : uint = 0
    for {
        if i >= arr.len() {
            break
        }
        n :: arr[i]
        println(`arr[{i}] = {arr[i]}`)
        i = i + 1
    }

    return 0
}

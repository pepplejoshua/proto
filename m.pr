struct A {
    num : int

    fn init(n int) void {
        self.num = n
    }
}


fn main() int {
    a :: new(A, 300); defer free(a)
    println(`{a.num}`)

    b :: some 1
    println(`{b.unwrap()}`)

    return 0
}

fn main() int {
    my_age :: 25
    label :: "sex"
    sex :: "male"

    hmap : {str, str} = {
        "name": "joshua",
        "age": `{my_age}`,
        label: sex
    }

    for [key, value] in hmap {
        println(key + ": " + value)
    }

    println(`{hmap}`)

    return 0
}

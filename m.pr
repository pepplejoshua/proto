fn main() int {
    a :: add(1, 2);
    println(`{a}`);
    println(`{fib(14)}`);
    return 0;
}

fn add(a int, b int) int {
    return a + b;
}

fn fib(n int) int {
    if n <= 1 {
        return n;
    }

    return fib(n - 1) + fib(n - 2);
}

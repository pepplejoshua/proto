@lib("std");
@use("next");

// constants
a : int : 1;
b :: a + 2;

// variables
c := b + a + 4;

// functions
main :: () -> void {
  calc : (int) -> int : next.forward;
}

// struct type
user :: {
  name : str;
  age : int;
}

// code module
maths :: mod {
  add :: (a : int, b : int) -> int {
    return a + b;
  }

  sub :: (a : int, b : int) -> int {
    return a - b;
  }
}
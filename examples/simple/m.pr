main :: fn () void {
    b :: 2;
    // why is this working???
    // it isn't working. it does not error on a
    // reference to an ill defined variable
    a : int : b;
    c : int = b;
    d : str = a + b + 'a';
}

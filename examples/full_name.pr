struct Name {
    first_name: str,
    last_name: str,
}

fn concat(name: Name) -> str {
    name.first_name + " " + name.last_name
}

fn main() -> str {
    let name: Name = Name {
        first_name: "Joshua",
        last_name: "Pepple",
    };

    concat(name)
}
